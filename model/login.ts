import Usuario from './usuario';
export default class Login {
    constructor() {}

    validar = (usuario: Usuario): Promise<any> => {
        return new Promise((resolve, reject) => {
            
        });
    }
}